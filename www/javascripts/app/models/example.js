/**
 * @module models/ExampleModel
 */

define(['jquery', 'underscore', 'backbone'],
  function($, _, Backbone) {

    'use strict';

    var ExampleModel = Backbone.Model.extend({

      'defaults': {},

      'initialize': function() {}

    });

    return ExampleModel;
  }
);
